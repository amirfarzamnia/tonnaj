export type CartTypes = {
    title?: string;
    id?: string;
    image?: string;
    author?: string;
    locationName?: string;
    description?: string;
    buttonHref?: string;
};

export type AuthTypes = {
    phone_number: string;
};
